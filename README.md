## 📖 Basic Discord Economy Bot for WPG

A small and lightweight bot built with Discord.js that provides a simple economy system for War-Political Gaming (WPG) themed servers.

### 💡 Main Features

* Virtual currency with commands:

  * `/balance` – shows the user’s current balance
* In-server shop with item purchasing and selling (`/shop`, `/buy`, `/sell`)
* User inventory management (`/inventory`)
* Money and item transfer between users (`/pay`, `/give`)
* And many other different commands!

### 🛠 Technical Details

* Built with **Discord.js**
* Config in **JSON files** 
* Minimal dependencies (only 1–2 packages)
* SqLite 3 database
* Fast to set up and run – just configure the token and commands

### 🚀 Installation

1. Clone the repository
2. Install dependencies:

   ```bash
   npm install
   ```
3. Add your token and other in `config.json`
4. Start the bot:

   ```bash
   node main.js
   ```

---
